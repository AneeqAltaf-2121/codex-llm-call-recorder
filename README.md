# CodexProbe

Transparent LLM call recorder and backend swap harness for Codex CLI.

> This README is a placeholder populated during early scaffolding. The full
> documentation (architecture, quick start, Codex configuration, Qwen setup,
> log schema, Docker, tests, experimental results) is written in
> [Phase 17](docs/architecture.md) once the implementation is complete.

## Status

Project scaffold in progress. See `docs/` and `src/codex_probe/` as
development proceeds phase by phase.
